Testing
ICT171 — Ashfaqul Haque Bhuiyan (35720354)
This document records the results of functional testing performed on the ShiftTrack server and website.

Testing Table
#TestMethodExpected ResultActual ResultStatus1Website loads via IP addressOpen http://20.2.88.235 in browserShiftTrack page loads with live clock[INSERT RESULT][INSERT ✓/✗]2Website loads via DNS nameOpen http://[INSERT DNS NAME] in browserShiftTrack page loads[INSERT RESULT][INSERT ✓/✗]3Live clock updatesObserve #liveClock element for 3 secondsTime increments each second[INSERT RESULT][INSERT ✓/✗]4Date and time panel updatesObserve #currentDate and #currentTimeShows correct local date and time[INSERT RESULT][INSERT ✓/✗]5Clock In — no name enteredLeave worker name blank, click Clock InError message: "Please enter a worker name"[INSERT RESULT][INSERT ✓/✗]6Clock In — valid nameEnter name, click Clock InStatus changes to "Clocked In", Clock Out button enables, elapsed timer starts[INSERT RESULT][INSERT ✓/✗]7Elapsed timer counts upClock in and watch elapsed time displayTimer increments each second[INSERT RESULT][INSERT ✓/✗]8Clock OutAfter clocking in, click Clock OutShift saved, duration shown in message, Clock In re-enabled[INSERT RESULT][INSERT ✓/✗]9Duration calculated correctlyClock in, wait 2 minutes, clock outDuration shows approximately 2m[INSERT RESULT][INSERT ✓/✗]10Shift record appears in tableComplete a clock-in/out cycleNew row appears in shift history table[INSERT RESULT][INSERT ✓/✗]11Multiple shifts recordedComplete 3 clock-in/out cyclesTable shows 3 rows, newest first[INSERT RESULT][INSERT ✓/✗]12Total hours calculatedComplete 2 shifts of known durationTotal hours badge shows correct sum[INSERT RESULT][INSERT ✓/✗]13Shift history persists on refreshAdd shifts, refresh the page (F5)Shift history still visible after reload[INSERT RESULT][INSERT ✓/✗]14Delete individual shiftClick "✕ Remove" on a row, confirmThat row is removed; others remain[INSERT RESULT][INSERT ✓/✗]15Clear all historyClick "Clear All", confirmAll rows removed; empty state message shown[INSERT RESULT][INSERT ✓/✗]16Script report page loadsOpen http://20.2.88.235/shift-report.htmlServer health report page displays[INSERT RESULT][INSERT ✓/✗]17Report shows correct uptimeCompare report uptime to uptime -p on serverValues match[INSERT RESULT][INSERT ✓/✗]18Report shows Apache status as RunningCheck report badgeGreen "Running" badge visible[INSERT RESULT][INSERT ✓/✗]19Report shows HTTP 200 OKCheck HTTP Check badge on report pageGreen "200 OK" badge visible[INSERT RESULT][INSERT ✓/✗]20Apache service is runningsudo systemctl status apache2Output shows active (running)[INSERT RESULT][INSERT ✓/✗]21HTTPS works (if configured)Open https://[INSERT DNS NAME]Page loads with padlock icon, no certificate error[INSERT RESULT][INSERT ✓/✗]22HTTP redirects to HTTPS (if configured)Open http://[INSERT DNS NAME]Automatically redirects to https://[INSERT RESULT][INSERT ✓/✗]

Test Environment
SettingValueServer OSUbuntu 24.04 LTSWeb ServerApache 2Browser[INSERT BROWSER AND VERSION]Test Date[INSERT TEST DATE]TesterAshfaqul Haque Bhuiyan (35720354)

Notes

Tests 21 and 22 (HTTPS) only apply if Let's Encrypt SSL has been configured (see ssl-setup.md).
Test 13 (localStorage persistence) requires JavaScript to be enabled in the browser.
All tests were performed from a browser accessing the live Azure VM.
